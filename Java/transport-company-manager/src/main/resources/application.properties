# MySQL connection for the main (development) database
db.url=jdbc:mysql://localhost:3306/transport_company?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.user=root
db.pass=password

# This project uses Hibernate auto DDL update (for easier local run).
# In real systems, use migrations (Flyway/Liquibase).

# If you want to run on a different DB, change the values above or pass system properties:
# -Ddb.url=... -Ddb.user=... -Ddb.pass=...
